% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compress_indicators.R
\name{compress_indicators}
\alias{compress_indicators}
\title{Compress retained IIS/SIS indicator patterns in an estimated gets/isat model}
\usage{
compress_indicators(
  final_model,
  min_iis_run_length = 3,
  same_sign_ratio = 3,
  sis_reversal_tolerance = 0.25,
  criterion = c("BIC", "AIC"),
  iterate = TRUE,
  max_iter = 10,
  require_diagnostics_not_worse = FALSE,
  diagnostic_pval_floor = 0.01,
  verbose = FALSE
)
}
\arguments{
\item{final_model}{An estimated \code{gets::isat} model object.}

\item{min_iis_run_length}{Integer. Minimum number of consecutive same-sign IIS
indicators required before proposing an IIS-to-SIS compression. Default is 3.}

\item{same_sign_ratio}{Numeric. Maximum allowed ratio between the largest and
smallest absolute IIS coefficient within a same-sign run. Default is 3.}

\item{sis_reversal_tolerance}{Numeric. Tolerance for treating adjacent
opposite-sign SIS indicators as approximately cancelling. The pair is
considered a reversal if \code{abs(beta1 + beta2) <= tolerance * max(abs(beta1), abs(beta2))}.
Default is 0.25.}

\item{criterion}{Character. Either \code{"BIC"} or \code{"AIC"}. Default is \code{"BIC"}.}

\item{iterate}{Logical. If TRUE, repeatedly searches for further improvements
after each accepted compression. Default is TRUE.}

\item{max_iter}{Integer. Maximum number of compression iterations. Default is 10.}

\item{require_diagnostics_not_worse}{Logical. If TRUE, do not accept a
compression if available AR/ARCH diagnostic p-values get worse than
\code{diagnostic_pval_floor}. Default is FALSE.}

\item{diagnostic_pval_floor}{Numeric. Minimum acceptable diagnostic p-value
when \code{require_diagnostics_not_worse = TRUE}. Default is 0.01.}

\item{verbose}{Logical. If TRUE, print accepted modifications. Default is FALSE.}
}
\value{
A list with the original model, compressed model, accepted
modifications, all attempted candidates, and criterion values.
}
\description{
This function post-processes an already selected \code{gets::isat} model. It looks
for two common indicator patterns:
}
\details{
\enumerate{
\item consecutive IIS indicators with the same sign, which may be more
parsimoniously represented as a single SIS indicator;
\item adjacent SIS indicators with opposite signs and similar magnitudes, which
may be more parsimoniously represented as a single IIS indicator.
}

Candidate replacements are accepted only if they improve the selected
information criterion, by default BIC.
}
\keyword{internal}
