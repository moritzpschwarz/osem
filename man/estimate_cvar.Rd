% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate_cvar.R
\name{estimate_cvar}
\alias{estimate_cvar}
\title{Estimate the specific CVAR sub-system using the Johansen procedure}
\usage{
estimate_cvar(
  clean_data,
  system_name,
  dep_vars_basename,
  x_vars_basename,
  use_logs,
  ar,
  coint_deterministic = c("none", "const", "trend"),
  coint_significance = c("1pct", "5pct", "10pct")
)
}
\arguments{
\item{clean_data}{An input data.frame or tibble. Must be the output of
clean_data() to fit all requirements.}

\item{system_name}{A character string specifying the name of the CVAR
sub-system.}

\item{dep_vars_basename}{A character string of the names of the dependent
variable separated by a comma, and contained in clean_data() in a level form
(i.e. no ln or D in front of the name).}

\item{x_vars_basename}{A character vector of the name(s) of the independent
variable(s), and contained in clean_data() in a level form (i.e. no ln or D
in front of the name). Can be an empty string if no exogenous regressors are
included.}

\item{use_logs}{To decide whether to log any variables. Must be one of
'both', 'y', 'x', or 'none'. Default is 'both'.}

\item{ar}{Number of lags of the VAR system in levels. Must be > 2.}

\item{coint_deterministic}{Character specifying whether and if yes which
deterministic component to include in the cointegrating relationship.}

\item{coint_significance}{Significance level for the rank test for
cointegration.}

\item{freq}{Data frequency if want to include seasonal dummies, NULL
otherwise.}
}
\value{
A list containing unit root tests for the variables involved, the
resulting CVAR model, and the argument setting:
\item{cointtest}{An object of \link[urca]{ca.jo-class} with the
cointegration test result.}
\item{vecm}{A list storing the restricted vector error correction model as
returned by \code{\link[urca:cajorls]{urca::cajorls()}}.}
\item{varm}{An object storing the restricted vector autoregressive model in
levels as returned by \code{\link[vars:vec2var]{vars::vec2var()}}.}
\item{rank}{The CVAR rank as determined by Johansen's trace test.}
\item{urtest}{A list of two lists storing the augmented Dickey-Fuller unit
root tests for all x and y variables as class \link[urca]{ur.df-class}.}
\item{args}{A list storing the settings of the function call.}
}
\description{
Estimate the specific CVAR sub-system using the Johansen procedure
}
