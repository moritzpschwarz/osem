# Updates the osem dataset with fitted values

NOTE: exported for now, later will be internal

## Usage

``` r
update_data(orig_data, new_data)
```

## Arguments

- orig_data:

  Original data that is to be updated.

- new_data:

  New dataset that will update the original data.
